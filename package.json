{
  "name": "jenkins-gateway",
  "version": "0.1.0",
  "description": "Local Jenkins HTTP API gateway with CLI and stdio MCP server.",
  "type": "module",
  "bin": {
    "jenkins-gateway": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "README.zh.md",
    "SECURITY.md",
    "docs/manual.en.md",
    "docs/manual.zh.md",
    "skills",
    "config/environments/*.example.env",
    ".env.example",
    ".codex/jenkins-mcp.example.toml"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration",
    "prepack": "npm run build"
  },
  "keywords": [
    "mcp",
    "jenkins",
    "cli",
    "codex",
    "model-context-protocol"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
